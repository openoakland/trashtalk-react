import React, { PureComponent } from 'react';
import { bindActionCreators } from 'redux';
import { connect } from 'react-redux';
import PropTypes from 'prop-types';

const styles = {
};

/**
 * This represents a tool category and all of its related tools
 */
export default class ToolCategory extends PureComponent {
  static propTypes = {
  }

  static defaultProps = {
  }

  render() {
    return (
      <div>Connected Component</div>
    );
  }
}
